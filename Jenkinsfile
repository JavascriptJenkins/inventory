// Define a pipeline job
pipeline {
    agent any // Run on any available agent

    stages {
        stage('Hello') { // Define a single stage named 'Hello'
            steps {
                echo 'Hello, World!' // Print "Hello, World!" to the console
            }
        }
    }
}
